import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleContainer: {
    flexDirection: "row",
    alignItems: "center",
    gap: 8,
    marginBottom: 12,
  },
  page: {
    flex: 1,
    paddingTop: 49,
    padding: 24,
    gap: 16,
  },
  heroContainer: {
    gap: 8,
    marginBottom: 16,
  },
  actionsContainer: {
    gap: 12,
    marginBottom: 16,
  },
});

export default styles;
